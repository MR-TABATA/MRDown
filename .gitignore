# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright MCP scratch output
.playwright-mcp/

# Raw Playwright captures; the encoded results live in docs/media/
demo/out/

# Strategy notes (freemium boundary, priorities). Deliberately not published —
# this belongs in .gitignore rather than .git/info/exclude, which lives on one
# machine only and would leave a fresh clone one `git add -A` away from
# committing it.
/notes/
